extend <%= rootDir %>base

block content
  //- Provides layout level markup
  .layout-wrapper<% if (name) { %>.<%= _.slugify(name.toLowerCase()) %><% } %>
    block <%= _.slugify(name.toLowerCase()) %>
