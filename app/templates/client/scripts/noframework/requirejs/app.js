/**
*   Application Logic
*/

define(function(require, exports, module) {
    'use strict';

    // Alias the module for easier identification.
    var app = module.exports;

    // Set global functionality to `app` namespace below
    // i.e. app.root = '/'

    app.welcome = 'Welcome to Yeogurt';

});
