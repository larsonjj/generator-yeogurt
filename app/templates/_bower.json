{
  "name": "<%= _.slugify(projectName) %>",
  "version": "0.0.1",
  "dependencies": {
    "consolelog": "~2.0.1",
    "html5shiv": "~3.7.2",
    "modernizr": "~2.8.1",
    "normalize.css": "~3.0.1"<% if (jsFramework === 'angular') { %>,
    "angular": "~1.2.28",
    "angular-route": "~1.2.28",
    "json3": "~3.3.1"<% } %><% if (jsFramework === 'backbone' && useAuth) { %>,
    "jquery.cookie": "~1.4.1"<% } %><% if (jsFramework !== 'react') { %>,
    "jquery": "~1.11.1"<% } %><% if (jsFramework === 'backbone') { %>,
    "backbone": "~1.1.2"<% } %><% if (jsFramework === 'react') { %>,
    "es5-shim": "~3.4.0"<% } %><% if (jsTemplate === 'handlebars') { %>,
    "handlebars": "~2.0.0"<% } else if (jsTemplate === 'jade') { %>,
    "jade": "~1.5.0"<% } %><% if (jsOption === 'requirejs') { %>,
    "requirejs": "~2.1.11"<% } %>
  },
  "devDependencies": {<% if (jsFramework === 'angular') { %>
    "angular-mocks": ">=1.2.*"
  <% } %>}
}
