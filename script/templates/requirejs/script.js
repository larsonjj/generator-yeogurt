/**
*   <%= _.classify(name) %> Script Description
*/

define(function(require) {

    'use strict';

    var init = function() {
        // Intialize module
    };

    return {
        init: init
    };

});
