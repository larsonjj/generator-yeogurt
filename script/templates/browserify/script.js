/**
*   <%= _.classify(name) %> Script Description
*/

'use strict';

var <%= _.classify(name) %> = (function() {

    var init = function() {
         // Intialize module
    };

    return {
        init: init
    };

})();

module.exports = <%= _.classify(name) %>;
